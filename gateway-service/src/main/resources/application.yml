server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes: []

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 10s

sa-token:
  token-name: satoken
  timeout: 2592000
  token-style: uuid
  is-log: true
