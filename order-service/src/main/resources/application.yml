server:
  port: 8082

spring:
  application:
    name: order-service

# Knife4j Swagger 配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
  # 通过网关访问时的前缀
  server-prefix: /orders